services:
  caddy:
    image: caddy:alpine
    ports:
      - '8081:80'
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile:ro
      - ./html:/usr/share/nginx/html:ro
      - ./dist:/usr/share/nginx/listingway:ro
    restart: unless-stopped
